// Decklist runtime config.
// After you deploy the backend (the Express server), put its URL here, e.g.:
//   window.__DECKLIST_API__ = "https://decklist-api.onrender.com";
// Leave it as "" for local dev (the Vite proxy handles /api and /uploads).
// You can edit this file in the built dist/ without rebuilding.
window.__DECKLIST_API__ = "";
